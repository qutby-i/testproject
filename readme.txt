git status -

git commit -m "Erste Datei wurde hinzugefügt"

git add readme.txt
git config --global useer.name "Kubilay Atmis"
git config --global useer.email "k.atmis@t-online.de"

$ git commit -a -m "noch mehr Dateien"

vim-befehle:
http://www.selflinux.org/selflinux/pdf/vim.pdf

Nach Veränderungen sieht alles etwas anders aus! So auch hier.
Finde die Veränderung! Du erhälst ..

Eine Änderung in/aus der <temp-branch> - fine.
